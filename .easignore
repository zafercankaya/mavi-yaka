# Browser profiles and temp files
.claude/
.git/
.pw-*/
.playwright-mcp/

# Apps not needed for mobile build (web, admin)
apps/web/
apps/admin/

# Apps not needed for mobile build
apps/api/
apps/crawler/

# Logs and temp
*.log
logs_tmp/

# Signing files at root (credentials.json refs them relatively)
# Keep dist_cert.p12, dist_profile.mobileprovision, AuthKey in apps/mobile/

# Large binary files at root
/*.aab
/*.ipa
/*.zip
/*.png
/*.p8
/*.sh
/*.xlsx

# Root temp scripts
/*.js

# Root temp JSON (keep essential)
/*.json
!package.json
!package-lock.json
!eas.json
!tsconfig.json
